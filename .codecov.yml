comment: false  # Don't comment on PRs - gets noisy
coverage:
  status:
    project:
      default:
        informational: true  # Don't enfore project coverage for now, make strict later
    patch:
      default:
        informational: true  # Don't enfore PR coverage for now, make strict later
